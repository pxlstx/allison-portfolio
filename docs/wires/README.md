# Wireframes

Drop layout references here (PNG, PDF, or sketches). The portfolio scaffold uses a clean editorial layout until wires are added.

Suggested mapping:

- `home.*` → `/`
- `work.*` → `/work` and `/work/[slug]`
- `writing.*` → `/writing`
- `ask.*` → `/ask`
